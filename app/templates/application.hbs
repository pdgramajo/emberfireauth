  <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          {{#link-to 'categories' class="navbar-brand"}} Home {{/link-to}}
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          
           {{#if session.isAuthenticated}}
              <form class="navbar-form navbar-right" >
                <div class="form-group">
                  <label style="color:white;"> Logged in as {{session.currentUser.email}} </label>
                </div>
                <button class="btn btn-default" {{action "signOut"}}>Sign out</button>
              </form>
          {{else}}
          <form class="navbar-form navbar-right" >
            <div class="form-group">
              {{input type="text" placeholder="Email" class="form-control" value=email required=true }}
            </div>
            <div class="form-group">
              {{input type="password" placeholder="Password" class="form-control" value=password required=true}}
            </div>
            <button {{action "signIn" }} class="btn btn-success">Sign in</button>
          </form>
          {{/if}}
        </div><!--/.navbar-collapse -->
      </div>
    </nav>

    <!-- Main jumbotron for a primary marketing message or call to action -->
    <div class="jumbotron">
      <div class="container">
        <h1>Hello, world!</h1>
      </div>
    </div>

    <div class="container">
      
    {{outlet}}
    
      <footer>
        <p>&copy; 2016 Company, Inc.</p>
      </footer>
    </div> <!-- /container -->

